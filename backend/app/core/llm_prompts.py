"""Все служебные промпты для LLM-вызовов в одном месте."""

NICHE_DETECTOR_PROMPT = """Определи нишу и тип бизнеса по РЕАЛЬНОМУ содержимому сайта.

URL сайта: {url}
Название бренда: {brand_name}
Определённый регион: {region}

Реальный контент главной страницы (опирайся на него, а НЕ на название домена):
\"\"\"
{site_content}
\"\"\"

ВАЖНО про регион:
- Если «Определённый регион» указан — используй его, НЕ меняй.
- Если регион пуст/неясен — определи по контенту (города, валюта, телефоны).
- НЕ предполагай Россию по умолчанию. Русскоязычный сайт ≠ российский
  (может быть Беларусь, Казахстан и т.д.). Если регион неясен — верни "unknown".

ВАЖНО про тип бизнеса (`business_type`):
- "service" — компания оказывает услуги (бухгалтерский аутсорс, ремонт, клиника)
- "product" — продаёт физические товары
- "saas" — программный продукт/платформа (1С, Контур, CRM)
- "media" — СМИ/блог/контент
Это нужно, чтобы конкурентов подбирать ТОГО ЖЕ типа (услуге — услуги, не SaaS).

Верни СТРОГО JSON:
{{
  "brand": "название компании/бренда, как оно написано на сайте",
  "category": "краткая категория бизнеса (1-3 слова на русском)",
  "subcategory": "уточняющая подкатегория",
  "business_type": "service / product / saas / media",
  "region": "регион работы (город, страна) или unknown",
  "target_audience": "B2B / B2C / B2B2C",
  "target_audience_description": "краткое описание целевой аудитории",
  "language": "ru / en / ru-en",
  "is_local": true,
  "typical_user_questions": ["3 примера запросов, которые задают потенциальные клиенты этого бизнеса в ИИ"]
}}

Только JSON, без пояснений и markdown-обёртки."""


COMPETITOR_FINDER_PROMPT = """Бизнес-контекст:
- Категория: {category}
- Подкатегория: {subcategory}
- Регион: {region}
- Целевая аудитория: {target_audience_description}
- Бренд клиента (исключить): {brand_name}

Назови 5 главных конкурентов клиента — реальные компании, которые работают в этой нише и регионе.
- Бренды должны быть известными и узнаваемыми.
- Не включай {brand_name}.
- Если регион Россия/Беларусь — приоритет российским/белорусским брендам.

Верни СТРОГО JSON-массив строк с точными названиями брендов:
["Название1", "Название2", "Название3", "Название4", "Название5"]

Только JSON, без пояснений."""


REAL_QUERIES_SELECTOR_PROMPT = """Перед тобой — РЕАЛЬНЫЕ поисковые запросы живых людей.
Источник: автоподсказки Google и Яндекс, Wordstat. Это то, что люди РЕАЛЬНО набирают.
НО: в списке много мусора — смежные товары, другие категории, нерелевантное.
Твоя задача — отсеять нерелевантное и отобрать только то, что РЕАЛЬНО про бизнес клиента.

Контекст бизнеса клиента:
- Категория: {category}
- Подкатегория (узкая ниша): {subcategory}
- Регион: {region}
- Аудитория: {target_audience_description}
- Бренд клиента (исключить, если фигурирует): {brand_name}

Задача: отобрать до {count} запросов, **которые задаст потенциальный клиент
именно этого бизнеса**. Покрытие типов намерения если есть в списке:
- рекомендательные («посоветуй ...», «какой лучше ...», «топ ...»)
- сравнительные («что лучше — ... или ...», «чем отличается ... от ...»)
- проблемные («как решить ...», «куда обратиться ...»)
- транзакционные («сколько стоит ...», «где купить ...»)

ЖЁСТКИЕ ПРАВИЛА (нарушение → результат отбрасывается валидатором):
1. ОТФИЛЬТРОВАТЬ НЕРЕЛЕВАНТНОЕ. Если запрос про смежный товар, другую категорию,
   игры, другую марку, бытовые батарейки вместо автомобильных — ИСКЛЮЧИТЬ.
   Например, для магазина АВТОМОБИЛЬНЫХ аккумуляторов нерелевантно:
   «аккумуляторы AA», «аккумуляторы для PS5», «аксессуары террария»,
   «аксессуары BMW» (это запчасти, не аккумуляторы).
2. Используй ТОЛЬКО формулировки из списка ниже, ДОСЛОВНО — символ в символ.
3. НЕ придумывай новые запросы. НЕ переформулируй.
4. НЕ ставь двоеточий и префиксов «Сравнение:», «X или Y:», «Рекомендация:».
5. ЛУЧШЕ ВЕРНУТЬ МЕНЬШЕ, ЧЕМ МУСОР. Если релевантных всего 5 — верни 5,
   не добивай нерелевантными до {count}.
6. Если по какому-то типу намерения в списке нет — пропусти его, не выдумывай.

Реальные запросы:
{real_queries}

Верни СТРОГО JSON-массив строк, каждая — ДОСЛОВНО из списка выше:
["запрос 1", "запрос 2", ...]

Только JSON."""


PROMPT_GENERATOR_PROMPT = """Сгенерируй 10 разнообразных запросов, которые потенциальные клиенты задают ИИ-ассистентам (ChatGPT, YandexGPT, Алиса) в этой нише.

Контекст:
- Категория: {category}
- Подкатегория: {subcategory}
- Регион: {region}
- Целевая аудитория: {target_audience_description}

Распределение типов запросов:
- 4 рекомендательных («посоветуй ...», «какую выбрать», «топ-N в ...»)
- 3 сравнительных — РЕАЛЬНЫМИ формулировками: «аутсорс бухгалтерии или штатный
  бухгалтер что лучше», «чем отличается ... от ...». КАТЕГОРИЧЕСКИ НЕ пиши «X или Y:»
  как буквальный префикс — это шаблон, в живом поиске так не пишут.
- 2 проблемных («как решить...», «куда обратиться, если...»)
- 1 транзакционный («где заказать ...», «сколько стоит ...»)

Требования к запросам:
- Естественный разговорный русский язык, как реальные пользователи в чате с ИИ.
  Со СТРОЧНОЙ буквы (как в поиске), без шаблонных префиксов «Сравнение:» /
  «Рекомендация:» / «X или Y:». Никаких двоеточий в начале запроса.
- ОБЯЗАТЕЛЬНО: все запросы релевантны региону «{region}». Для локального бизнеса
  включай географию прямо в запрос (город/страну). Пример для «бухгалтерия, Витебск»:
  «посоветуй бухгалтерскую компанию на аутсорсе в Витебске», а НЕ «лучшая бухгалтерия».
- Учитывай локальную специфику (законодательство, валюту, площадки региона).
- Никаких упоминаний конкретных брендов в запросах.

Верни СТРОГО JSON-массив из 10 строк:
["запрос 1", "запрос 2", ..., "запрос 10"]

Только JSON."""


MENTION_ANALYZER_PROMPT = """Перед тобой — ответ ИИ-ассистента на запрос пользователя.

Запрос: "{prompt}"

Ответ ИИ:
\"\"\"
{response_text}
\"\"\"

Список брендов для анализа: {brands_json}

Для каждого бренда из списка определи:
1. mentioned: true/false — упомянут ли бренд (учитывай разные написания, опечатки, переводы)
2. position: число от 1 до N — какой по счёту упомянут (1 = первый, 0 = не упомянут)
3. sentiment: тональность упоминания —
   - "positive": ИИ рекомендует, выделяет, ставит в топ, хвалит
   - "neutral": просто упомянут в перечислении без явной оценки
   - "negative": ИИ предупреждает о бренде, критикует или ставит в негативном контексте
4. context: фрагмент текста (до 150 символов) с упоминанием бренда
5. is_recommendation: true/false — упомянут как РЕКОМЕНДАЦИЯ или просто перечислен

Также извлеки:
- citations: массив URL/доменов, которые упомянуты в ответе как источники

Верни СТРОГО JSON:
{{
  "brands": [
    {{
      "name": "название бренда",
      "mentioned": true,
      "position": 1,
      "sentiment": "positive",
      "context": "фрагмент текста",
      "is_recommendation": true
    }}
  ],
  "citations": ["url1", "url2"]
}}

Только JSON."""


RECOMMENDATIONS_PROMPT = """На основе анализа видимости бренда в ИИ-ассистентах сгенерируй 5 конкретных рекомендаций для улучшения GEO-показателей.

Бренд: {brand_name}
Ниша: {category} ({subcategory})
Регион: {region}

Текущие метрики:
- AI Visibility Score: {score}/100
- Доля упоминаний: {presence_rate}%
- Худшие модели (где не упомянут): {weak_models}
- Сильные модели (где упомянут): {strong_models}
- Главные конкуренты, обгоняющие клиента: {top_competitors}
- Источники, на которые ссылаются ИИ в нише: {top_sources}

Авторитетные площадки региона клиента (для рекомендаций по внешним упоминаниям бери ТОЛЬКО их): {platforms}

Требования к рекомендациям:
- Конкретные, не абстрактные (например, "разместить экспертный кейс на {first_platform}" вместо "улучшить контент")
- Когда советуешь внешние публикации/упоминания — называй площадки ТОЛЬКО из списка выше.
  НЕ предлагай площадки других стран (например, НЕ предлагай vc.ru/habr белорусскому бизнесу).
- Приоритизированы по impact: от самой важной к менее важной
- Каждая рекомендация — 1-3 предложения с обоснованием

Верни СТРОГО JSON:
{{
  "recommendations": [
    {{
      "priority": 1,
      "title": "короткий заголовок",
      "description": "описание что и зачем",
      "expected_impact": "ожидаемый эффект",
      "effort": "low"
    }}
  ]
}}

Только JSON."""
